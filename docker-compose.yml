services:

  # PHP service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /var/www/
    volumes:
      - ./:/var/www
    environment:
      PHP_MEMORY_LIMIT: 1024M
    command:
      - php
      - -d
      - memory_limit=${PHP_MEMORY_LIMIT}
      - base.php
